# Leave unset (or empty) during local dev so the app uses the Vite dev server
# and proxies /api + /cable to Rails (see vite.config.js). Fixes "Failed to fetch" from CORS.
#
# VITE_API_URL=http://localhost:3000

# Optional: if Rails runs elsewhere during dev
# VITE_PROXY_API=http://127.0.0.1:3000
